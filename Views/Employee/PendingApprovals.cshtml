@model IEnumerable<LeaveRequest>

@{
    ViewData["Title"] = "Pending Leave Approvals";
}

<!-- General styles -->
<link rel="stylesheet" href="~/css/ListPageHeader.css" />
<link rel="stylesheet" href="~/css/CRUD/Index.css" />
<link rel="stylesheet" href="~/css/GeneralModal.css" />

<div class="page-section">
    <!-- Header -->
    <div class="bp-header">
        <div class="bp-header-content">
            <div>
                <h2><i class="bi bi-clipboard-check"></i> Pending Leave Approvals</h2>
                <p>Review and process employee leave requests awaiting action</p>
            </div>
            <div>
                <a asp-action="TeamLeaveCalendar" class="bp-back-btn btn-lg">
                    <i class="bi bi-calendar3"></i> Team Calendar
                </a>
            </div>
        </div>
    </div>

    <!-- Alert Messages -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle"></i> @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle"></i> @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Statistics Cards (Unified Style) -->
    <div class="bp-stats-row mb-4">
        <div class="bp-stat-card" style="background: var(--card1-bg);">
            <div>
                <h3 style="color: var(--card1-h3);">@Model.Count()</h3>
                <p style="color: var(--card1-p);">Pending Requests</p>
                <small style="color: var(--card1-p);">Awaiting approval</small>
            </div>
            <i class="bi bi-clock-history" style="color: var(--card1-icon);"></i>
        </div>
        <div class="bp-stat-card" style="background: var(--card2-bg);">
            <div>
                <h3 style="color: var(--card2-h3);">@Model.Select(r => r.EmployeeId).Distinct().Count()</h3>
                <p style="color: var(--card2-p);">Employees</p>
                <small style="color: var(--card2-p);">Requesting leave</small>
            </div>
            <i class="bi bi-people" style="color: var(--card2-icon);"></i>
        </div>
        <div class="bp-stat-card" style="background: var(--card3-bg);">
            <div>
                <h3 style="color: var(--card3-h3);">@Model.Sum(r => r.TotalDays).ToString("F1")</h3>
                <p style="color: var(--card3-p);">Total Days</p>
                <small style="color: var(--card3-p);">Requested</small>
            </div>
            <i class="bi bi-calendar3" style="color: var(--card3-icon);"></i>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="bp-search-card shadow-sm border-0 mb-4">
        <div class="card-body p-3 d-flex gap-2 flex-wrap">
            <a asp-action="TeamLeaveCalendar" class="quick-action-btn card3">
                <i class="bi bi-calendar3 me-2"></i><span>Team Calendar</span>
            </a>
        </div>
    </div>

    <!-- Pending Requests Table -->
    @if (Model.Any())
    {
        <div class="card">
            <div class="card-header bg-warning text-dark">
                <h5 class="mb-0"><i class="bi bi-list-check"></i> Requests Awaiting Approval</h5>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="table-light">
                            <tr>
                                <th>Employee</th>
                                <th>Department</th>
                                <th>Leave Type</th>
                                <th>Start Date</th>
                                <th>End Date</th>
                                <th>Days</th>
                                <th>Reason</th>
                                <th>Submitted</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var request in Model.OrderBy(r => r.CreatedDate))
                            {
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            @if (!string.IsNullOrEmpty(request.Employee.ImageURL))
                                            {
                                                <img src="@request.Employee.ImageURL" alt="@request.Employee.FirstName" 
                                                     class="rounded-circle me-2" style="width: 40px; height: 40px; object-fit: cover;" />
                                            }
                                            else
                                            {
                                                <div class="rounded-circle bg-primary text-white d-inline-flex align-items-center justify-content-center me-2" 
                                                     style="width: 40px; height: 40px; font-size: 1.2rem;">
                                                    @request.Employee.FirstName?.Substring(0, 1).ToUpper()
                                                </div>
                                            }
                                            <div>
                                                <strong>@request.Employee.FirstName @request.Employee.LastName</strong>
                                                <br />
                                                <small class="text-muted">@request.Employee.JobTitle?.Name</small>
                                            </div>
                                        </div>
                                    </td>
                                    <td>@request.Employee.Department?.Name</td>
                                    <td>
                                        <i class="bi bi-calendar-event text-primary"></i>
                                        @request.LeaveType.Name
                                    </td>
                                    <td>@request.StartDate.ToString("dd MMM yyyy")</td>
                                    <td>@request.EndDate.ToString("dd MMM yyyy")</td>
                                    <td>
                                        <span class="badge bg-info">@request.TotalDays.ToString("F1") days</span>
                                    </td>
                                    <td>
                                        @if (!string.IsNullOrEmpty(request.Reason))
                                        {
                                            <span data-bs-toggle="tooltip" title="@request.Reason">
                                                @(request.Reason.Length > 30 ? request.Reason.Substring(0, 30) + "..." : request.Reason)
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td>
                                        @request.CreatedDate.ToString("dd MMM yyyy")
                                        <br />
                                        <small class="text-muted">@request.CreatedDate.ToString("HH:mm")</small>
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a asp-action="LeaveRequestDetails" asp-route-id="@request.Id" 
                                               class="btn btn-sm btn-info" title="View Details">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                            <button type="button" class="btn btn-sm btn-success" 
                                                    data-bs-toggle="modal" data-bs-target="#approveModal@(request.Id)" 
                                                    title="Approve">
                                                <i class="bi bi-check-circle"></i>
                                            </button>
                                            <button type="button" class="btn btn-sm btn-danger" 
                                                    data-bs-toggle="modal" data-bs-target="#rejectModal@(request.Id)" 
                                                    title="Reject">
                                                <i class="bi bi-x-circle"></i>
                                            </button>
                                        </div>

                                        <!-- Approve Modal -->
                                        <div class="modal fade" id="approveModal@(request.Id)" tabindex="-1">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header bg-success text-white">
                                                        <h5 class="modal-title">Approve Leave Request</h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <p>Are you sure you want to approve this leave request?</p>
                                                        <div class="alert alert-info">
                                                            <strong>Employee:</strong> @request.Employee.FirstName @request.Employee.LastName<br />
                                                            <strong>Leave Type:</strong> @request.LeaveType.Name<br />
                                                            <strong>Period:</strong> @request.StartDate.ToString("dd MMM yyyy") - @request.EndDate.ToString("dd MMM yyyy")<br />
                                                            <strong>Days:</strong> @request.TotalDays.ToString("F1")<br />
                                                            @if (!string.IsNullOrEmpty(request.Reason))
                                                            {
                                                                <strong>Reason:</strong> @request.Reason
                                                            }
                                                        </div>
                                                    </div>
                                                    <div class="modal-footer">
                                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                                        <form asp-action="ApproveLeaveRequest" asp-route-id="@request.Id" method="post" style="display: inline;">
                                                            @Html.AntiForgeryToken()
                                                            <button type="submit" class="btn btn-success">
                                                                <i class="bi bi-check-circle"></i> Approve
                                                            </button>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Reject Modal -->
                                        <div class="modal fade" id="rejectModal@(request.Id)" tabindex="-1">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header bg-danger text-white">
                                                        <h5 class="modal-title">Reject Leave Request</h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <p>Are you sure you want to reject this leave request?</p>
                                                        <div class="alert alert-warning">
                                                            <strong>Employee:</strong> @request.Employee.FirstName @request.Employee.LastName<br />
                                                            <strong>Leave Type:</strong> @request.LeaveType.Name<br />
                                                            <strong>Period:</strong> @request.StartDate.ToString("dd MMM yyyy") - @request.EndDate.ToString("dd MMM yyyy")<br />
                                                            <strong>Days:</strong> @request.TotalDays.ToString("F1")<br />
                                                            @if (!string.IsNullOrEmpty(request.Reason))
                                                            {
                                                                <strong>Reason:</strong> @request.Reason
                                                            }
                                                        </div>
                                                    </div>
                                                    <div class="modal-footer">
                                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                                        <form asp-action="RejectLeaveRequest" asp-route-id="@request.Id" method="post" style="display: inline;">
                                                            @Html.AntiForgeryToken()
                                                            <button type="submit" class="btn btn-danger">
                                                                <i class="bi bi-x-circle"></i> Reject
                                                            </button>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="alert alert-success">
            <i class="bi bi-check-circle"></i> <strong>Great!</strong> No pending leave requests at the moment.
        </div>
    }
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Initialize tooltips
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
            var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                return new bootstrap.Tooltip(tooltipTriggerEl)
            })
        });
    </script>
}
